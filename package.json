{
  "name": "helios-volume-bot",
  "version": "2.0.0",
  "description": "Testnet volume bot for Helios Trade (AsterSwap) on RISE Chain — swaps + liquidity",
  "main": "scripts/volumeBot.js",
  "type": "module",
  "scripts": {
    "start":     "node scripts/volumeBot.js --mode both",
    "swap":      "node scripts/volumeBot.js --mode swap",
    "liquidity": "node scripts/volumeBot.js --mode liquidity",
    "both":      "node scripts/volumeBot.js --mode both"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ethers": "^6.13.0"
  }
}
