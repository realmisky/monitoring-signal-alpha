{
  "name": "helios-smart-volume-bot",
  "version": "3.0.0",
  "description": "Smart-analyst volume bot for Helios Trade (AsterSwap) on RISE Testnet",
  "main": "scripts/volumeBot.js",
  "type": "module",
  "scripts": {
    "start":     "node scripts/volumeBot.js --mode auto",
    "auto":      "node scripts/volumeBot.js --mode auto",
    "swap":      "node scripts/volumeBot.js --mode swap",
    "liquidity": "node scripts/volumeBot.js --mode liquidity"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ethers": "^6.13.0"
  }
}
